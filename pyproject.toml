[project]
name = "atalaya"
version = "0.1.0"
description = "Atalaya — Plataforma de Attack Surface Management (ASM) con triaje por IA"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Andrew" }]

dependencies = [
    # API
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    # Base de datos
    "sqlalchemy>=2.0",
    "greenlet>=3.0",
    "asyncpg>=0.29",
    "aiosqlite>=0.20",
    "alembic>=1.13",
    # Descubrimiento / red
    "httpx>=0.27",
    "dnspython>=2.6",
    "cryptography>=42.0",
    # IA
    "anthropic>=0.34",
    "google-genai>=1.0",
    # Dashboard
    "streamlit>=1.37",
    # Informes
    "jinja2>=3.1",
    "xhtml2pdf>=0.2.16",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
    "mypy>=1.10",
]

[project.scripts]
atalaya = "atalaya.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint.flake8-bugbear]
# fastapi.Depends() en la firma de una ruta es el idiom estándar de
# inyección de dependencias de FastAPI, no una llamada mutable reusada
# por accidente entre invocaciones (lo que B008 previene de verdad).
extend-immutable-calls = ["fastapi.Depends"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
